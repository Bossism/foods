<?xml version="1.0" encoding="UTF-8"?>
<!DOCTYPE mapper PUBLIC "-//mybatis.org//DTD Mapper 3.0//EN" "http://mybatis.org/dtd/mybatis-3-mapper.dtd">
<mapper namespace="com.example.foods.mapper.CourierMapper">

    <select id="login" resultType="com.example.foods.entity.user.Courier">
        select * from w_courier where username=#{username} and password=#{password}
    </select>

</mapper>